const path = require('path');

module.exports = {
  "config": path.resolve('./server/config', 'config.json'),
  "models-path": path.resolve('./server/models/accounting'),
  "seeders-path": path.resolve('./server/seeders/accounting'),
  "migrations-path": path.resolve('./server/migrations/accounting')
};
