const path = require('path');

module.exports = {
  "config": path.resolve('./server/config', 'config.json'),
  "models-path": path.resolve('./server/models/programs'),
  "seeders-path": path.resolve('./server/seeders/programs'),
  "migrations-path": path.resolve('./server/migrations/programs')
};
